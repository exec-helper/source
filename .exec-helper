patterns:
    MODE:
        default-values:
            - dev
            #- release
        short-option: m
        long-option: mode
    TARGET:
        default-values:
            - x86_64-unknown-linux-gnu
            #- x86_64-unknown-linux-musl
        long-option: target
    FILTER:
        default-values:
            - ""
        short-option: f
        long-option: filter

commands:
    hello: Command to print hello to the console
    world: Print hello world! to the console
    build: Build the project for the given TARGET
    run: Run the project executable for the given TARGET
    release: Create a release build
    analyze: Analyze the source code
    setup: Setup the repository for building
    docs: Build + install the documentation
    changelog: Build the changelog
    format: Format and check the format of the sources

watch:
- src
- scripts
- Cargo.toml
- .exec-helper

hello:
#- command-line-command
- bash

world:
- hello
- command-line-command

build:
- cargo

run:
- cargo

create-installer:
- command-line-command

release:
- clean-release
- build-release

build-release:
- cargo

clean-release:
- cargo

format:
- rs-format
- check-changes

analyze:
- clippy
- format

clippy:
- cargo

rs-format:
- cargo

py-format:
- command-line-command

check-changes:
- command-line-command

docs:
- build-docs
- install-docs

build-docs:
- cmake

install-docs:
- cmake

command-line-command:
    world:
      patterns:
      - EH_ROOT_DIR
      - TARGET
      environment:
        EH: "exec-helper!"
      working-dir: /tmp
      command-line:
        - pattern: [ echo, "{TARGET}: ${EDITOR}@{EH_ROOT_DIR}"]
        - pwd: [ pwd]

    check-changes:
        command-line: [ git, diff-index, --quiet, HEAD, --]

bash:
    hello:
      command: 'echo "Hello"'

cargo:
  patterns:
  - TARGET
  - MODE
  - EH_ROOT_DIR
  target: "{TARGET}"
  profile: "{MODE}"
  color: always

  build:
    command: build

  run:
    profile: '{MODE}'
    environment:
      PLUGIN_DEFAULT_SEARCH_PATH: './scripts'   # TODO: replace {EH_ROOT_DIR} in environment variables
    command: run
    program-args: [ --force-color=true, --log-level=warn, --keep-output, hello, world]

  build-release:
    environment:
      PLUGIN_DEFAULT_SEARCH_PATH: '/usr/share/exec-helper/plugins'
    command: build
    profile: release

  clean-release:
    command: clean
    profile: release

  rs-format:
    patterns: []
    command: fmt

  clippy:
    patterns: []
    command: clippy
    all-targets: yes
    fail-on-warnings: true
