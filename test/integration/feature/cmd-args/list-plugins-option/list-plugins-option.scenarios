File: list-plugins-option-cmd-args
Scenario: The list plugins option is defined on a valid command line
    [Arguments]     @{args}
    Given a test environment with a valid config
    When we call the application with the list plugins option and other command line options which are not the help option    ${args}
    and the call should succeed
    Then the plugins information should be printed to stdout

File: list-plugins-option-cmd-args
Scenario: The list plugins option is defined on a valid command line with no configuration
    [Arguments]     @{args}
    Given a test environment with no config
    When we call the application with the list plugins option and other command line options which are not the help option    ${args}
    and the call should succeed
    Then the plugins information should be printed to stdout
