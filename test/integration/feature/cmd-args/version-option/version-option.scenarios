File: version-option-cmd-args
Scenario: The version option is defined on a valid command line
    [Arguments]     @{args}
    Given a test environment with a valid config
    When we call the application with the version option and other command line options which are not the help option    ${args}
    and the call should succeed
    Then the version information should be printed to stdout

File: version-option-cmd-args
Scenario: The version option is defined on a valid command line with no configuration
    [Arguments]     @{args}
    Given a test environment with no config
    When we call the application with the version option and other command line options which are not the help option    ${args}
    and the call should succeed
    Then the version information should be printed to stdout
